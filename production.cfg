[buildout]
extends =
    buildout.cfg

parts +=
    supervisord.conf

[supervisord.conf]
recipe = collective.recipe.template
input = etc/production/supervisord.conf.template
output = supervisord.conf
